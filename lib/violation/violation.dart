import 'package:flutter/material.dart';

class Violation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Violation'),
      ),
      body: Center(
        child: Text('This is the Violation screen'),
      ),
    );
  }
}
